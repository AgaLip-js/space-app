export interface LoginFormTitle {
    title: string;
}

export interface MyFormValues {
    email: string;
    password: string;
  }

export interface myRegisterFormValues {
    email: string;
    password: string;
    login: string;
}
